<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AqariSyriaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AqariSyria"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Splash -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AqariSyria.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".activities.LoginActivity" android:exported="false"/>
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>

        <!-- Main -->
        <activity android:name=".activities.MainActivity" android:exported="false"/>

        <!-- Property Detail -->
        <activity android:name=".activities.PropertyDetailActivity" android:exported="false"/>

        <!-- Add Property -->
        <activity android:name=".activities.AddPropertyActivity" android:exported="false"/>

        <!-- Search -->
        <activity android:name=".activities.SearchActivity" android:exported="false"/>
        <activity android:name=".activities.SearchResultsActivity" android:exported="false"/>

        <!-- Favorites -->
        <activity android:name=".activities.FavoritesActivity" android:exported="false"/>

        <!-- Profile -->
        <activity android:name=".activities.ProfileActivity" android:exported="false"/>

        <!-- Admin -->
        <activity android:name=".activities.AdminActivity" android:exported="false"/>

        <!-- Chat -->
        <activity android:name=".activities.ChatActivity" android:exported="false"/>

        <!-- Settings -->
        <activity android:name=".activities.SettingsActivity" android:exported="false"/>

        <!-- Notifications -->
        <activity android:name=".activities.NotificationsActivity" android:exported="false"/>

        <!-- My Properties -->
        <activity android:name=".activities.MyPropertiesActivity" android:exported="false"/>

        <!-- Comparison -->
        <activity android:name=".activities.ComparisonActivity" android:exported="false"/>

    </application>

</manifest>
